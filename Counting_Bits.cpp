#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
using ll = long long;
int a[1005];
void solve() {
    int n; cin >> n; 
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}